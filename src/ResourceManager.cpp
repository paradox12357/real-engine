#include "types.h"

using namespace std;
using namespace resource;

void ResourceManager::initializeResourceManager() {

}
void ResourceManager::shutdownResourceManager() {

}
string ResourceManager::getPath(string path) { // temporary function, will replace when needed.
	return path;
}